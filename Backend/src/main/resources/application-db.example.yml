# 复制为 application-local.yml 后填写真实账号密码（application-local.yml 已被 .gitignore 忽略）
#
# 腾讯云实例 cdb220732
# - 内网（同 VPC CVM）：172.27.0.14:3306
# - 外网（本机开发）：cd-cdb-83ftf614.sql.tencentcdb.com:25007

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:cd-cdb-83ftf614.sql.tencentcdb.com}:${DB_PORT:25007}/${DB_NAME:pixel_craft_forge}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:请填写腾讯云数据库密码}

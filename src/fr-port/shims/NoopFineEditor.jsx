import { Typography } from 'antd'

const { Text } = Typography

/** 精细画笔编辑器体量过大，此处仅占位；完整功能请用「精灵表调整」等工具 */
export default function ImageFineEditor() {
  return <Text type="secondary">精细画笔编辑器未接入本版，可使用下方预设缩放与抠图。</Text>
}
